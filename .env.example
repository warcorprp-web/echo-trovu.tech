# Upstream API Configuration
UPSTREAM_API_URL=https://api.ceiller.ru/openai-qwen-oauth/v1
UPSTREAM_API_KEY=879621

# Cache Settings
CACHE_THRESHOLD=0.88    # Semantic similarity threshold (0.70-0.95)
                        # 0.88 = conservative (fewer false positives)
                        # 0.70 = aggressive (more savings, some errors)
CACHE_TTL=3600
ENABLE_SEMANTIC=true

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
